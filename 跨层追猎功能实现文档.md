# 跨层追猎功能实现文档

## 一、功能概述

### 1.1 功能需求

在立方体（多楼层）游戏中，实现**跨层追猎**功能：

- 当怪物具有"追猎"特殊能力（special 包含 28）时
- 玩家在**同行或同列**移动一格（包括跨面移动）
- 怪物向玩家方向移动一格（立体上更近的方向）
- 如果立体上前后距离相等，则怪物不移动

### 1.2 典型场景示例

#### 场景1：同层追猎（基础场景）
- **怪物位置**：MT3 (5, 5)
- **玩家位置**：MT3 (7, 5)
- **触发条件**：玩家在同列（y=5）向右移动
- **预期效果**：怪物从 MT3(5,5) 移动到 MT3(6,5)（向右移动一格，靠近玩家）

#### 场景2：跨层追猎（直线扫描，方向不变）
- **怪物位置**：MT0 (12, 5)
- **玩家位置**：MT3 (1, 5)
- **触发条件**：玩家踩到 MT3(1,5)，怪物在 MT0(12,5) 的右方向直线扫描可到达
- **预期效果**：怪物从 MT0(12,5) 跨面到 MT3(0,5)（向右移动一格，靠近玩家）
- **关键**：MT0 right → MT3 left，方向都是水平（right/left），属于"直线扫描"，算同行

#### 场景3：坐标旋转不触发追猎
- **怪物位置**：MT4 (12, 12)
- **玩家位置**：MT3 (9, 0)
- **触发条件**：玩家从 MT4(12,9) 右走跨面到 MT3(9,0)
- **预期效果**：怪物不动
- **原因**：MT4 right → MT3 up，方向从水平（right）变为垂直（up），属于"坐标旋转"，不算同行同列

#### 场景4：距离相等不移动
- **怪物在立方体上的位置**：前后方向到达玩家的立体步数相等
- **预期效果**：怪物不移动（保持原位）

---

## 二、核心概念与技术原理

### 2.1 立方体结构

游戏采用**立方体结构**，6 个楼层（MT0-MT5）按立方体面排列：

| 楼层 | 名称 | 说明 |
|------|------|------|
| MT0 | 正面 | |
| MT1 | 后面 | |
| MT2 | 左面 | |
| MT3 | 右面 | |
| MT4 | 顶面 | |
| MT5 | 底面 | |

每个楼层是 13×13 的网格。

### 2.2 楼层邻接关系（CUBE_EDGES）

定义在 [project/functions.js](file:///d:/立方体/mota-js/project/functions.js#L1873-L1880) 和 [project/plugins.js](file:///d:/立方体/mota-js/project/plugins.js#L9728-L9735) 中：

```javascript
CUBE_EDGES = {
  MT0: { up: ["MT4", "down"], down: ["MT5", "up"],    left: ["MT2", "right"], right: ["MT3", "left"] },
  MT1: { up: ["MT5", "down"], down: ["MT4", "up"],    left: ["MT3", "right"], right: ["MT2", "left"] },
  MT2: { up: ["MT4", "left"], down: ["MT5", "left"],  left: ["MT1", "right"], right: ["MT0", "left"] },
  MT3: { up: ["MT4", "right"], down: ["MT5", "right"], left: ["MT0", "right"], right: ["MT1", "left"] },
  MT4: { up: ["MT1", "down"], down: ["MT0", "up"],    left: ["MT2", "up"],    right: ["MT3", "up"] },
  MT5: { up: ["MT0", "down"], down: ["MT1", "up"],    left: ["MT2", "down"],  right: ["MT3", "down"] }
}
```

格式：`源楼层.方向 = [目标楼层, 目标边]`

例如：`MT0.right = ["MT3", "left"]` 表示从 MT0 向右走出右边界，会从 MT3 的左边界进入。

### 2.3 关键函数

#### 1. `cubeStep(floorId, x, y, direction)`

- **位置**：[project/functions.js](file:///d:/立方体/mota-js/project/functions.js#L1881-L1899)
- **功能**：计算走一格后的位置（含跨面）
- **返回**：`{ floorId, x, y }` 或 `null`

#### 2. `cubeProject(floorId, x, y, dx, dy)`

- **位置**：[project/functions.js](file:///d:/立方体/mota-js/project/functions.js#L1902-L1920)
- **功能**：从 (x,y) 偏移 (dx,dy) 步，返回最终位置
- **实现**：先沿 x 方向走 abs(dx) 步，再沿 y 方向走 abs(dy) 步

#### 3. `getCheckBlock(floorId)`

- **位置**：[project/functions.js](file:///d:/立方体/mota-js/project/functions.js)
- **功能**：获取指定楼层的"阻激夹域"信息，包括 damage、type、repulse、ambush、chase 等
- **追猎检测**：在函数内的"立方体跨面处理"部分进行跨层追猎检测

#### 4. `_checkBlock_chase(chase)`

- **位置**：[libs/control.js](file:///d:/立方体/mota-js/libs/control.js#L1345-L1430)
- **功能**：执行追猎逻辑，控制怪物实际移动

---

## 三、实现架构

### 3.1 总体流程

```
玩家移动一格
    ↓
moveOneStep() 被调用
    ↓
core.checkBlock() 触发阻激夹域检测
    ↓
从 core.status.checkBlock.chase 中取出当前位置的追猎信息
    ↓
_checkBlock_chase() 执行追猎
    ↓
计算距离，选择最近方向（距离相等则不移动）
    ↓
怪物向玩家方向移动一格（同层或跨层）
```

### 3.2 检测与执行分离

跨层追猎采用**检测与执行分离**的架构：

| 阶段 | 函数 | 位置 | 职责 |
|------|------|------|------|
| 检测 | `getCheckBlock` 中的跨面处理 | [project/functions.js](file:///d:/立方体/mota-js/project/functions.js#L1647-L1705) | 扫描所有楼层的怪物，计算追猎信息，存入 `checkBlock.chase` |
| 执行 | `_checkBlock_chase` | [libs/control.js](file:///d:/立方体/mota-js/libs/control.js#L1345-L1430) | 读取 `checkBlock.chase`，计算距离，执行怪物移动 |

### 3.3 chase 数据结构

```javascript
// checkBlock.chase 的结构
chase = {
  "x,y": [  // 玩家位置坐标作为 key
    {
      x: 12,        // 怪物 x 坐标
      y: 5,         // 怪物 y 坐标
      dir: "right", // 追猎方向（怪物向这个方向移动）
      srcFloor: "MT0", // 怪物所在楼层
      step: 2       // 立体距离（从怪物位置到玩家位置的步数）
    },
    // ... 可能有多个怪物追猎同一位置
  ]
}
```

---

## 四、追猎检测逻辑（getCheckBlock）

### 4.1 本层追猎检测

遍历每个怪物，如果有追猎能力（special 含 28），向四个方向扫描，在每个可到达的位置记录追猎信息。

### 4.2 跨层追猎检测

#### 三步算法

**第一步**：收集四个方向到达当前层的最近 step

```javascript
var _projDir = function(srcF, sx, sy, dir, floorId) {
  var visited = {}, f = srcF, cx = sx, cy = sy;
  for (var step = 0; step < 200; step++) {
    var key = f + "," + cx + "," + cy;
    if (visited[key]) break;     // 防止无限循环
    visited[key] = true;
    var nextS = cubeStep(f, cx, cy, dir);
    if (!nextS) break;
    // 跨面后方向必须保持一致（直线扫描），否则停止
    if (nextS.floorId !== f && !_isStraightCross(f, dir)) break;
    f = nextS.floorId; cx = nextS.x; cy = nextS.y;
    if (f === floorId) return { dir: dir, step: step + 1 };
  }
  return null;
};
```

**第二步**：选 step 最小的方向；多个方向 step 相等则不记录（距离相等不移动）

```javascript
projections.sort(function(a, b) { return a.step - b.step; });
var minStep = projections[0].step;
var nearest = projections.filter(function(p) { return p.step === minStep; });
if (nearest.length === 1) {
  // 只有一个最近方向，记录追猎
  var bestDir = nearest[0].dir;
  // ...
}
// 多个方向距离相等，不记录（怪物不移动）
```

**第三步**：沿最近方向扫描，只记录落在当前层的位置

```javascript
var f = srcF, cx = sb.x, cy = sb.y;
for (var step = 0; step < 200; step++) {
  var s = cubeStep(f, cx, cy, bestDir);
  if (!s) break;
  if (s.floorId !== f && !_isStraightCross(f, bestDir)) break;
  f = s.floorId; cx = s.x; cy = s.y;
  if (f === floorId) {
    var ck = cx + "," + cy;
    if (!chase[ck]) chase[ck] = [];
    chase[ck].push({
      x: sb.x, y: sb.y, dir: bestDir,
      srcFloor: srcF, step: step + 1
    });
  }
}
```

### 4.3 直线扫描判定（_isStraightCross）

这是跨层追猎的**核心判定逻辑**：只有跨面后方向保持一致的，才算"立体同行/同列"，才触发追猎。

```javascript
var _isStraightCross = function(fromF, dir) {
  var e = core.control.controldata.CUBE_EDGES[fromF];
  if (!e || !e[dir]) return false;
  var te = e[dir][1];  // 目标边
  var dirH = (dir === "left" || dir === "right");     // 源方向是否水平
  var teH = (te === "left" || te === "right");        // 目标边是否水平
  return dirH === teH; // 都是水平或都是垂直，方向不变
};
```

#### 判定规则

| 源方向 | 目标边 | 类型 | 是否触发追猎 | 示例 |
|--------|--------|------|-------------|------|
| right（水平） | left（水平） | 直线扫描 | ✅ 触发 | MT0 right → MT3 left |
| left（水平） | right（水平） | 直线扫描 | ✅ 触发 | MT3 left → MT0 right |
| up（垂直） | down（垂直） | 直线扫描 | ✅ 触发 | MT0 up → MT4 down |
| down（垂直） | up（垂直） | 直线扫描 | ✅ 触发 | MT4 down → MT0 up |
| right（水平） | up（垂直） | 坐标旋转 | ❌ 不触发 | MT4 right → MT3 up |
| up（垂直） | left（水平） | 坐标旋转 | ❌ 不触发 | MT2 up → MT4 left |
| right（水平） | down（垂直） | 坐标旋转 | ❌ 不触发 | MT5 right → MT3 down |

---

## 五、追猎执行逻辑（_checkBlock_chase）

### 5.1 跨层与本层分离

```javascript
var crossLayer = chase.filter(c => c.srcFloor && c.srcFloor !== core.status.floorId);
var sameLayer = chase.filter(c => !c.srcFloor || c.srcFloor === core.status.floorId);
```

- **跨层追猎**：怪物在其他楼层，需要用 `cubeStep` 计算移动
- **本层追猎**：怪物在当前楼层，正常移动

### 5.2 跨层追猎执行

```javascript
if (crossLayer.length > 0) {
  crossLayer.forEach(c => {
    var stepTo = core.control.controldata.cubeStep(c.srcFloor, c.x, c.y, c.dir);
    if (!stepTo) continue;
    
    var sm = core.status.maps[c.srcFloor];
    var val = sm.map[c.y][c.x];
    
    // 检查目标位置是否为空
    if (!core.getBlock(stepTo.x, stepTo.y, stepTo.floorId, false)) {
      // 移动怪物到新位置
      var targetMap = core.status.maps[stepTo.floorId];
      if (!targetMap.map[stepTo.y]) targetMap.map[stepTo.y] = [];
      targetMap.map[stepTo.y][stepTo.x] = val;
      sm.map[c.y][c.x] = 0;
      
      // 清除缓存
      if (sm.blocks) sm.blocks = null;
      if (targetMap.blocks) targetMap.blocks = null;
      if (core.status.mapBlockObjs) {
        core.status.mapBlockObjs[stepTo.floorId] = null;
        core.status.mapBlockObjs[c.srcFloor] = null;
      }
    }
  });
  // 刷新地图
  actions.push({
    type: "function",
    async: true,
    function: "function(){core.redrawMap();core.doAction();}"
  });
}
```

### 5.3 距离相等不移动

在检测阶段（getCheckBlock），如果多个方向到达玩家的立体距离相等，就不记录追猎信息，怪物自然不会移动。

这实现了"立体距离相等则不移动"的需求。

---

## 六、跨面方向旋转（玩家视角）

### 6.1 问题描述

玩家从 MT4（顶面）向右走，跨面到 MT3（右面）后：
- **预期**：出现在 MT3(9,0)，方向向下
- **实际（旧版本）**：出现在 MT3(10,0)，方向向右

### 6.2 根因分析

1. **方向没旋转**：旧代码跨面时 `direction: dir` 保留原方向，没有根据目标边旋转
2. **按键长按循环**：`_sys_pressKey` 的 `setTimeout` 循环持续调用 `moveHero('right')`，每次把方向设回 right，导致跨面后又往右走了一步

### 6.3 修复方案

在 [project/plugins.js](file:///d:/立方体/mota-js/project/plugins.js#L9840-L9870) 的 `cubeMap` 插件中修复：

```javascript
control.prototype.moveAction = function (callback) {
  // ...
  if (target && target.crossed) {
    // 跨面后方向旋转
    var edge = edges[floorId][dir];
    var newDir = edge ? opposite[edge[1]] : dir;
    // 跨面前停止按键长按循环，防止旧方向覆盖新方向
    core.status.holdingKeys = [];
    core.status.heroStop = true;
    // route 记录新方向
    core.status.route.push(newDir);
    core.status.automaticRoute.lastDirection = newDir;
    core.changeFloor(target.floorId, null, 
      { x: target.x, y: target.y, direction: newDir }, 0, 
      function () {
        core.moveOneStep(callback);
        core.checkRouteFolding();
      });
    return;
  }
  // ...
};
```

### 6.4 方向旋转规则

跨面后方向 = `opposite(targetEdge)`（从目标边进入，朝远离该边的方向走）

| 源楼层 | 源方向 | 目标楼层 | 目标边 | 新方向 | 类型 |
|--------|--------|----------|--------|--------|------|
| MT0 | right | MT3 | left | right | 不变 |
| MT0 | left | MT2 | right | left | 不变 |
| MT0 | up | MT4 | down | up | 不变 |
| MT0 | down | MT5 | up | down | 不变 |
| MT4 | right | MT3 | up | down | 旋转 |
| MT4 | left | MT2 | up | down | 旋转 |
| MT2 | up | MT4 | left | right | 旋转 |
| MT3 | up | MT4 | right | left | 旋转 |

---

## 七、遇到的问题与解决方案

### 7.1 问题1：跨层追猎后怪物不移动

**现象**：怪物有追猎能力，玩家踩到对应位置，但怪物不动。

**根因**：`getMapBlocksObj` 有 `mapBlockObjs` 缓存，跨层移动后未清除缓存，导致后续追猎检测使用旧数据。

**修复**：跨层追猎执行后清除缓存：
```javascript
if (core.status.mapBlockObjs) {
  core.status.mapBlockObjs[stepTo.floorId] = null;
  core.status.mapBlockObjs[srcFloor] = null;
}
```

### 7.2 问题2：追猎方向错误（多方向记录）

**现象**：同一位置有多个追猎记录（不同方向），导致怪物移动方向混乱。

**根因**：使用 `reached` 标志导致离开当前层后仍记录同一坐标。

**修复**：改为仅当 `f === floorId` 时记录追猎信息。

### 7.3 问题3：坐标旋转的跨面也触发追猎

**现象**：玩家从 MT4 右走跨面到 MT3，MT4 的怪物也跟着跨面到 MT3，出现在奇怪的位置。

**根因**：旧版跨层追猎检测只要能到达当前层就记录，包括坐标旋转的跨面（MT4 right→MT3 up）。

**修复**：添加 `_isStraightCross` 判定，只有方向保持一致的跨面才算同行/同列。

### 7.4 问题4：跨面后方向不对，且多走一步

**现象**：玩家从 MT4(12,9) 右走，出现在 MT3(10,0)，方向向右（应该是 down，且在 (9,0)）。

**根因**：
1. 跨面时方向没旋转
2. 按键长按循环（`_sys_pressKey` 的 setTimeout）持续把方向设回 right
3. 方向变回 right 后又走了一步，导致从 (9,0) 走到 (10,0)

**修复**：
1. 跨面时方向旋转为 `opposite(targetEdge)`
2. 跨面前清空 `holdingKeys` 和设置 `heroStop = true`，阻止按键循环

---

## 八、测试验证

### 8.1 单元测试

测试文件：[tests/control.test.js](file:///d:/立方体/mota-js/tests/control.test.js)

测试用例：
1. `cubeStep 真实跨面逻辑` — 验证坐标计算正确
2. `跨层追猎检测逻辑` — 验证最近方向选择、距离相等不移动
3. `坐标旋转不触发追猎` — 验证 MT4→MT3 等坐标旋转场景不触发追猎
4. `方向旋转` — 验证跨面后方向计算正确
5. `_checkBlock_chase 执行逻辑` — 验证怪物实际移动正确
6. `完整流程` — 检测+执行的端到端验证

运行命令：
```bash
npx vitest run tests/control.test.js
```

### 8.2 手动测试场景

1. **同层追猎**：玩家在同层同行/同列移动，怪物追猎一格
2. **跨层追猎（直线扫描）**：MT0 怪物追猎到 MT3，方向不变
3. **坐标旋转不追猎**：MT4 右走跨面到 MT3，MT4 的怪物不追猎
4. **距离相等不移动**：怪物前后方向距离相等，不移动
5. **跨面方向旋转**：从 MT4 右走，到 MT3 后方向向下，位置正确
6. **长按跨面**：长按方向键跨面，不会多走一步

---

## 九、相关文件

| 文件 | 作用 | 关键行 |
|------|------|--------|
| [project/functions.js](file:///d:/立方体/mota-js/project/functions.js) | 追猎检测（getCheckBlock 中跨面处理）、cubeStep、CUBE_EDGES | L1647-L1705（跨层追猎检测）、L1873-L1899（cubeStep + CUBE_EDGES） |
| [libs/control.js](file:///d:/立方体/mota-js/libs/control.js) | 追猎执行（_checkBlock_chase） | L1345-L1430 |
| [project/plugins.js](file:///d:/立方体/mota-js/project/plugins.js) | cubeMap 插件（玩家跨面移动、方向旋转） | L9712-L9945 |
| [tests/control.test.js](file:///d:/立方体/mota-js/tests/control.test.js) | 单元测试 | 全文 |
| [main.js](file:///d:/立方体/mota-js/main.js) | 版本号（控制缓存） | L6 |

---

## 十、设计要点总结

1. **检测与执行分离**：getCheckBlock 只生成追猎信息，_checkBlock_chase 负责执行
2. **直线扫描判定**：只有跨面后方向保持一致的，才算立体同行/同列，才触发追猎
3. **距离相等不移动**：检测阶段就排除距离相等的情况，不记录追猎信息
4. **跨面方向旋转**：玩家跨面后方向需要根据目标边旋转（opposite(targetEdge)）
5. **按键循环打断**：跨面前清空 holdingKeys，防止长按循环用旧方向覆盖新方向
6. **缓存清理**：怪物跨层移动后需要清除 mapBlockObjs 缓存，确保后续检测准确
