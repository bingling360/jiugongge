# 用户提示词汇总

本文件汇总了本次会话中用户给 AI 助手的所有主要提示词/指令。

---

## 1. 初始问题：跨层移动异常

**原始提示词**：

> 跨层移动的时候，是不是会有额外移动，怎么感觉不太对劲

**意图**：排查立方体跨层移动时怪物是否有额外移动的问题。

---

## 2. 详细问题描述

**原始提示词**：

> 应该说立方体的逻辑不对，我从顶面往右面走，为什么追猎会跟到右面去，还出现在一个很奇怪的位置，我从顶面(12,9)往右面走，走到了右面(10,0)，怪物就从在顶面的(12,12)，走到了右面的(12,0)，这是为什么，它本来应该不动的，另外还有一个问题，我从顶面(12,9)往右走，应该出现在(9,0)才对，怎么是(10,0)呢，然后到了右面主角方向应该是向下的才对，怎么方向是向右，这几个问题都帮我解决一下，然后测试一下符不符合我的要求

**意图**：

- 修复跨层追猎错误触发问题（坐标旋转的跨面不应触发追猎）
- 修复玩家跨面坐标错误（实际到了 `(10,0)`，应为 `(9,0)`）
- 修复玩家跨面方向错误（实际方向为 `right`，应为 `down`）
- 要求测试验证修复是否符合需求

---

## 3. 方向问题反馈

**原始提示词**：

> 方向转换有问题

**意图**：跨面后方向旋转没有生效，需要继续排查。

---

## 4. 调试反馈：方向未转换

**原始提示词**：

> 实际是朝右

**意图**：确认到方向确实没有转换，仍然是 `right`，帮助定位问题。

---

## 5. 缓存排查反馈

**原始提示词**：

> 还是不行，没用，控制台依然是原样

**意图**：第一次修复后未生效，反馈调试结果，要求继续排查。

---

## 6. 排除缓存因素

**原始提示词**：

> 不是缓存问题吧，我都重新打开本地服务器了都不行

**意图**：用户认为不是浏览器缓存导致的问题，要求从代码逻辑层面继续排查真正原因。

---

## 7. 文档记录需求

**原始提示词**：

> 帮我把实现跨层追猎的过程总结成文档，并用md格式的文件记录下来

**意图**：要求生成 Markdown 文档，记录跨层追猎功能的完整实现过程。

---

## 8. 提示词汇总需求

**原始提示词**：

> 帮我汇总一下我的提示词

**意图**：汇总本次会话中用户给 AI 的所有指令/提示词。

---

## 9. 导出为 Markdown 文档

**原始提示词**：

> 把总结变成一个md文档

**意图**：将提示词汇总内容保存为 Markdown 文件。

---

## 附录：需求对应关系

| 提示词 | 对应功能/问题 | 相关文件 |
|--------|--------------|----------|
| 跨层移动异常 | 跨层追猎重复移动问题 | `project/functions.js`、`libs/control.js` |
| 详细问题描述 | 坐标旋转不触发追猎、跨面坐标、跨面方向 | `project/plugins.js`、`project/functions.js` |
| 方向转换有问题 | 跨面方向旋转逻辑 | `project/plugins.js` |
| 控制台依然是原样 | 版本号缓存问题 | `main.js`、`index.html` |
| 不是缓存问题 | 按键长按循环覆盖方向 | `project/plugins.js`、`libs/actions.js` |
| 实现跨层追猎文档 | 生成实现文档 | `跨层追猎功能实现文档.md` |
| 汇总提示词 | 生成本文件 | `用户提示词汇总.md` |
