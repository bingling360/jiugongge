# 跨层阻击功能实现文档

## 一、功能概述

### 1.1 功能需求
在立方体（多楼层）游戏中，实现**跨层阻击**功能：
- 当怪物具有"阻击"特殊能力（special包含18）时
- 怪物和玩家处于**相邻楼层**的边缘位置
- 玩家踩到对应位置（跨层伤害格）时：
  1. **产生伤害**：玩家受到怪物阻击伤害
  2. **触发移动**：怪物向远离玩家的方向移动一格（在怪物所在楼层移动）

### 1.2 典型场景示例
- **怪物位置**：0层 (0, 6)
- **玩家位置**：2层 (12, 6)
- **触发条件**：玩家踩到2层(12,6)
- **预期效果**：
  - 玩家受到阻击伤害
  - 怪物从0层(0,6)移动到0层(1,6)（向右移动一格，远离玩家）

---

## 二、核心概念与技术原理

### 2.1 坐标系与楼层关系

#### 立方体结构
游戏采用**立方体结构**，多个楼层（MT0-MT5）按立方体面排列：
- 每个楼层是一个 13×13 的网格
- 楼层之间通过**边缘**连接（类似立方体的相邻面）
- 玩家可以从一个楼层走到相邻楼层的对应位置

#### 关键坐标转换函数

**1. `cubeProject(floorId, x, y, dx, dy)`**
- **功能**：计算从指定楼层的坐标向某方向移动后，投影到哪个楼层的哪个坐标
- **参数**：
  - `floorId`：源楼层ID（如 "MT0"）
  - `x, y`：源坐标
  - `dx, dy`：方向偏移（如 `{x:1, y:0}` 表示向右）
- **返回**：`{floorId: string, x: number, y: number}` 或 `null`
- **用途**：判断从当前位置向某方向移动，是否会"跨面"到相邻楼层

**示例**：
```javascript
// 从 MT0(0,6) 向左移动（dx=-1）
// 因为 x=0 已经是左边界，会跨面到相邻楼层
cubeProject("MT0", 0, 6, -1, 0)
// 返回：{floorId: "MT2", x: 12, y: 6}
```

**2. `cubeStep(floorId, x, y, direction)`**
- **功能**：计算从指定坐标向某方向走一步后的**实际位置**（自动处理跨面）
- **参数**：
  - `floorId`：当前楼层
  - `x, y`：当前坐标
  - `direction`：方向（"up"/"down"/"left"/"right"）
- **返回**：`{floorId: string, x: number, y: number}` 或 `null`
- **用途**：获取怪物被推后的最终位置（可能在同层，也可能跨层）

**示例**：
```javascript
// 怪物在 MT0(0,6)，向右推一格
cubeStep("MT0", 0, 6, "right")
// 返回：{floorId: "MT0", x: 1, y: 6}
// 解释：没跨面，还在MT0，x从0变成1
```

### 2.2 数据流与执行流程

#### 完整流程
```
玩家移动
    ↓
checkBlock (检查四周格)
    ↓
getCheckBlock (计算伤害和事件)
    ↓
  跨层处理（检查相邻楼层）
    ↓
  记录 damage (伤害) 和 repulse (阻击移动)
    ↓
checkBlock 继续执行
    ↓
_checkBlock_repulse (执行阻击移动)
    ↓
  怪物移动完成
```

#### 关键数据结构

**1. `damage` 对象**
- **格式**：`{ "x,y": damageValue }`
- **作用**：记录每个位置受到的伤害
- **跨层示例**：`{ "12,6": 50 }` 表示玩家在(12,6)受到50点伤害

**2. `repulse` 对象**
- **格式**：`{ "x,y": [ [x, y, id, rdir, srcFloor, destFloor, destX, destY] ] }`
- **作用**：记录阻击移动信息
- **数组元素含义**：
  - `x, y`：怪物当前坐标（源楼层）
  - `id`：怪物事件ID
  - `rdir`：阻击方向（怪物面向玩家的方向）
  - `srcFloor`：怪物所在楼层（源楼层）
  - `destFloor`：被推到的楼层
  - `destX, destY`：被推到的坐标
- **跨层示例**：
  ```javascript
  repulse["12,6"] = [
    [0, 6, "enemy001", "right", "MT0", "MT0", 1, 6]
  ]
  // 解释：0层(0,6)的怪物，被向右推到0层(1,6)
  ```

---

## 三、代码修改详解

### 3.1 修改文件清单

| 文件名 | 路径 | 修改内容 |
|--------|------|----------|
| functions.js | `project/functions.js` | 记录跨层阻击的伤害和移动信息 |
| control.js | `libs/control.js` | 执行跨层阻击的移动逻辑 |

---

### 3.2 `project/functions.js` 修改详解

#### 修改位置
- **函数**：`getCheckBlock()`
- **行号**：约1661-1695行
- **功能**：计算玩家移动时，四周格子触发的事件（伤害、阻击、追猎等）

#### 修改前的问题
**原代码**（约1673-1688行）：
```javascript
// 移动：计算怪物被推后出现在源楼层的哪个位置
var rdir = core.turnDirection(":back", rd);
// 怪物在源楼层向 rdir 方向推一格，计算目标位置
var step = core.control.controldata.cubeStep(srcF, sb.x, sb.y, rdir);
if (step && step.floorId == floorId) {  // ❌ 问题所在
    repulse[rk] = (repulse[rk] || []).concat([
        sb.x, sb.y, sb.event.id, rdir,
        srcF,
        step.floorId,
        step.x, step.y
    ]);
}
```

**问题分析**：
1. **条件错误**：`step.floorId == floorId` 永远为false
   - `step.floorId` 是源楼层（如 "MT0"）
   - `floorId` 是玩家楼层（如 "MT2"）
   - 两者不可能相等，导致跨层移动**永远不会被记录**

2. **数组格式错误**：使用 `.concat([...])` 会展平数组
   - 期望：`repulse[rk] = [ [0,6,"id","right","MT0","MT0",1,6] ]`
   - 实际：`repulse[rk] = [ 0, 6, "id", "right", "MT0", "MT0", 1, 6 ]`
   - 后续处理时会出错（以为是8个独立值，实际应该是1个数组元素）

#### 修改后的代码
```javascript
// 移动：计算怪物被推后出现在源楼层的哪个位置
var rdir = core.turnDirection(":back", rd);
// 怪物在源楼层向 rdir 方向推一格，计算目标位置
var pushTo = core.control.controldata.cubeStep(srcF, sb.x, sb.y, rdir);
if (pushTo) {  // ✅ 移除错误条件，只要能推就记录
    // 写入跨层移动记录：怪物从 srcF 被推到 pushTo 的位置
    // 注意：使用 push，不要用 concat（避免数组被展平）
    if (!repulse[rk]) repulse[rk] = [];
    repulse[rk].push([  // ✅ 使用 push，保持数组结构
        sb.x, sb.y, sb.event.id, rdir,
        srcF,           // 源楼层
        pushTo.floorId, // 被推到的楼层
        pushTo.x, pushTo.y  // 被推到的坐标
    ]);
}
```

**修改要点**：
1. **移除错误条件**：删除 `step.floorId == floorId`，只要 `pushTo` 有效就记录
2. **修复数组格式**：使用 `.push([...])` 而不是 `.concat([...])`
3. **数据完整性**：`pushTo` 可能等于 `srcF`（同层推），也可能不同（跨面推），都能正确处理

#### 代码执行流程示例

**场景**：怪物在0层(0,6)，玩家在2层(12,6)

1. **玩家移动到2层(12,6)**
2. **调用 `getCheckBlock()`**
3. **跨层处理**（检查相邻楼层0层）：
   - 发现0层(0,6)有阻击怪物
   - 计算投影：`cubeProject("MT0", 0, 6, -1, 0)` → `{floorId:"MT2", x:12, y:6}`
   - 确认投影位置 == 玩家位置（12,6），触发跨层阻击
4. **记录伤害**：
   ```javascript
   damage["12,6"] = 50;  // 假设阻击伤害为50
   ```
5. **记录移动**：
   ```javascript
   rdir = core.turnDirection(":back", "left");  // 玩家从左边来，怪物向右推
   pushTo = core.control.controldata.cubeStep("MT0", 0, 6, "right");
   // 返回：{floorId:"MT0", x:1, y:6}
   
   repulse["12,6"] = [
     [0, 6, "enemy001", "right", "MT0", "MT0", 1, 6]
   ];
   ```

---

### 3.3 `libs/control.js` 修改详解

#### 修改位置1：删除跨层追猎
- **函数**：`_checkBlock_chase()`
- **行号**：约1356-1378行（已删除）
- **功能**：原本支持追猎怪物跨层移动，现删除，只允许本层追猎

**删除的代码**：
```javascript
// ❌ 已删除的跨层追猎代码
chase.forEach((currChaseInfo) => {
    const { x, y, dir, srcFloor } = currChaseInfo;
    // 跨层追猎：怪物不在当前楼层
    if (srcFloor && srcFloor !== core.status.floorId) {
        var sm = core.status.maps[srcFloor];
        var dm = core.status.maps[core.status.floorId];
        if (sm && sm.map && dm && dm.map && sm.map[y] && sm.map[y][x]) {
            var val = sm.map[y][x];
            var targetX = hx - core.utils.scan[dir].x;
            var targetY = hy - core.utils.scan[dir].y;
            if (targetX >= 0 && targetX < core.floors[core.status.floorId].width && 
                targetY >= 0 && targetY < core.floors[core.status.floorId].height) {
                if (!dm.map[targetY]) dm.map[targetY] = [];
                if (!dm.map[targetY][targetX]) {
                    dm.map[targetY][targetX] = val;
                    sm.map[y][x] = 0;
                    if (sm.blocks) sm.blocks = null;
                    if (dm.blocks) dm.blocks = null;
                    actions.push({ "type": "move", "loc": [targetX, targetY], "time": 80, "keep": true, "async": true, "steps": [dir + ":1"] });
                }
            }
        }
        return;  // ❌ 这会导致跨层阻击的 return 失效
    }
    // ... 本层追猎代码
});
```

**删除原因**：
1. 用户不需要追猎跨层功能
2. 原代码中的 `return` 语句会导致跨层阻击代码也被跳过（都在同一个 `forEach` 中）

#### 修改位置2：实现跨层阻击移动逻辑
- **函数**：`_checkBlock_repulse()`
- **行号**：约1261-1315行
- **功能**：执行阻击移动（包括本层和跨层）

**修改后的代码**：
```javascript
control.prototype._checkBlock_repulse = function (repulse) {
    if (!repulse || repulse.length == 0) return [];
    var actions = [];
    repulse.forEach(function (t) {
        // 跨层阻击：t.length >= 8 且 t[4] 是字符串（源楼层ID）
        if (t.length >= 8 && typeof t[4] === 'string') {
            try {
                // t = [x, y, id, rdir, srcFloor, destFloor, destX, destY]
                var srcFloor = t[4], destFloor = t[5];
                var rdir = t[3];
                
                var sm = core.status.maps[srcFloor];
                if (sm && sm.map && sm.map[t[1]]) {
                    var val = sm.map[t[1]][t[0]] || 0;
                    if (val) {
                        // 使用 cubeStep 计算怪物被推后应该在的位置
                        if (!core.control.controldata.cubeStep) {
                            console.error('cubeStep not found');
                            return;
                        }
                        var pushTo = core.control.controldata.cubeStep(srcFloor, t[0], t[1], rdir);
                        
                        if (pushTo && pushTo.floorId && pushTo.x !== undefined && pushTo.y !== undefined) {
                            // 获取目标楼层的地图数据
                            var targetMap = core.status.maps[pushTo.floorId];
                            if (targetMap && targetMap.map) {
                                // 检查目标位置是否为空
                                if (!core.getBlock(pushTo.x, pushTo.y, pushTo.floorId, false)) {
                                    // 移动怪物到新位置
                                    if (!targetMap.map[pushTo.y]) targetMap.map[pushTo.y] = [];
                                    targetMap.map[pushTo.y][pushTo.x] = val;
                                    sm.map[t[1]][t[0]] = 0;
                                    
                                    // 清除缓存
                                    if (sm.blocks) sm.blocks = null;
                                    if (targetMap.blocks) targetMap.blocks = null;
                                }
                            }
                        }
                    }
                }
            } catch(e) {
                console.error('跨层阻击执行出错:', e);
            }
            return;
        }
        // 正常本层阻击
        actions.push({ "type": "move", "loc": [t[0], t[1]], "steps": [t[3]], "time": 80, "keep": true, "async": true });
    });
    if (actions.length > 0) actions.push({ "type": "waitAsync" });
    return actions;
}
```

**代码详解**：

1. **识别跨层阻击**：
   ```javascript
   if (t.length >= 8 && typeof t[4] === 'string')
   ```
   - 本层阻击：`t = [x, y, id, rdir]`（长度=4）
   - 跨层阻击：`t = [x, y, id, rdir, srcFloor, destFloor, destX, destY]`（长度=8）
   - 通过 `t[4]` 是否为字符串来判断是否为跨层阻击

2. **获取源楼层地图数据**：
   ```javascript
   var sm = core.status.maps[srcFloor];
   ```
   - `sm` 是源楼层的地图数据（怪物当前所在楼层）
   - 例如：`srcFloor = "MT0"`，则 `sm` 是0层的地图

3. **读取怪物值**：
   ```javascript
   var val = sm.map[t[1]][t[0]] || 0;
   ```
   - `t[1]` = y坐标，`t[0]` = x坐标
   - `val` 是地图格子的值（怪物ID或0）

4. **计算推后位置**：
   ```javascript
   var pushTo = core.control.controldata.cubeStep(srcFloor, t[0], t[1], rdir);
   ```
   - 使用 `cubeStep` 计算怪物被推后的位置
   - 可能的结果：
     - 同层推：`{floorId: "MT0", x: 1, y: 6}`
     - 跨面推：`{floorId: "MT2", x: 12, y: 6}`

5. **执行移动**：
   ```javascript
   if (pushTo && pushTo.floorId && pushTo.x !== undefined && pushTo.y !== undefined) {
       var targetMap = core.status.maps[pushTo.floorId];
       if (targetMap && targetMap.map) {
           if (!core.getBlock(pushTo.x, pushTo.y, pushTo.floorId, false)) {
               // 在目标位置创建怪物
               if (!targetMap.map[pushTo.y]) targetMap.map[pushTo.y] = [];
               targetMap.map[pushTo.y][pushTo.x] = val;
               // 在原位置删除怪物
               sm.map[t[1]][t[0]] = 0;
               // 清除缓存（强制重新渲染）
               if (sm.blocks) sm.blocks = null;
               if (targetMap.blocks) targetMap.blocks = null;
           }
       }
   }
   ```
   - **关键**：怪物可能被推到源楼层，也可能被推到另一个楼层（跨面）
   - **灵活性**：代码同时支持同层推和跨面推

6. **错误处理**：
   ```javascript
   try {
       // ... 跨层阻击逻辑
   } catch(e) {
       console.error('跨层阻击执行出错:', e);
   }
   ```
   - 防止游戏卡死
   - 出错时在控制台显示错误信息

#### 执行流程示例

**场景**：怪物在0层(0,6)，玩家在2层(12,6)，玩家踩到(12,6)

1. **`checkBlock()` 调用 `_checkBlock_repulse()`**
2. **传入参数**：`repulse = { "12,6": [[0, 6, "enemy001", "right", "MT0", "MT0", 1, 6]] }`
3. **识别跨层阻击**：
   - `t = [0, 6, "enemy001", "right", "MT0", "MT0", 1, 6]`
   - `t.length = 8`，`typeof t[4] = "string"` → 跨层阻击
4. **获取源楼层数据**：
   - `srcFloor = "MT0"`
   - `sm = core.status.maps["MT0"]`
5. **读取怪物值**：
   - `val = sm.map[6][0]`（0层(0,6)的格子值）
6. **计算推后位置**：
   - `pushTo = cubeStep("MT0", 0, 6, "right")`
   - 返回：`{floorId: "MT0", x: 1, y: 6}`
7. **执行移动**：
   - `targetMap = core.status.maps["MT0"]`（同层）
   - 检查 `(1,6)` 是否为空：`!core.getBlock(1, 6, "MT0", false)`
   - 如果为空：
     - `sm.map[6][1] = val`（在0层(1,6)创建怪物）
     - `sm.map[6][0] = 0`（删除0层(0,6)的怪物）
     - 清除缓存：`sm.blocks = null`
8. **移动完成**：怪物从0层(0,6)移动到0层(1,6)

---

## 四、关键技术点详解

### 4.1 方向计算

#### `core.turnDirection(":back", rd)`
- **功能**：计算相反方向
- **示例**：
  - `core.turnDirection(":back", "left")` → `"right"`
  - `core.turnDirection(":back", "up")` → `"down"`

#### 阻击方向逻辑
- **玩家从左边接近** → 怪物向右推（`rdir = "right"`）
- **玩家从上面接近** → 怪物向下推（`rdir = "down"`）

### 4.2 地图数据结构

#### `core.status.maps[floorId]`
- **结构**：
  ```javascript
  {
    map: [
      [0, 0, 0, 1001, 0, ...],  // y=0 行
      [0, 1002, 0, 0, 0, ...],  // y=1 行
      ...
    ],
    blocks: null,  // 缓存，为null时重新计算
    ...
  }
  ```
- **访问方式**：`map[y][x]`（注意：先y后x）

#### 地图值含义
- `0`：空地
- `正数`：怪物ID（如1001、1002等）
- 通过 `core.getBlock(x, y, floorId)` 可以获取解析后的块数据

### 4.3 缓存机制

#### `blocks` 缓存
- **作用**：缓存地图的块数据，提高渲染性能
- **修改地图后必须清除**：
  ```javascript
  if (sm.blocks) sm.blocks = null;
  ```
- **原理**：下次访问时重新计算 `blocks`

---

## 五、测试验证

### 5.1 测试场景

#### 场景1：基本跨层阻击
- **怪物**：0层(0,6)，有阻击能力
- **玩家**：2层(12,6)
- **预期**：
  - 玩家受到50点伤害
  - 怪物移动到0层(1,6)

#### 场景2：跨层阻击+跨面推
- **怪物**：0层(12,6)，有阻击能力
- **玩家**：2层(0,6)
- **预期**：
  - 玩家受到50点伤害
  - 怪物被向左推，跨面到2层(0,6)（因为0层x=12是右边界）

#### 场景3：本层阻击（不受影响）
- **怪物**：0层(6,6)，有阻击能力
- **玩家**：0层(6,5)
- **预期**：
  - 玩家受到50点伤害
  - 怪物移动到0层(6,7)（向右推）

### 5.2 常见问题排查

#### 问题1：游戏卡死
- **原因**：代码抛出未捕获的异常
- **解决**：检查控制台错误信息，确认是否有 `跨层阻击执行出错` 日志

#### 问题2：伤害生效，移动不生效
- **原因**：`functions.js` 的 `repulse` 记录格式错误
- **排查**：
  ```javascript
  // 在浏览器控制台执行
  console.log(core.status.checkBlock.repulse);
  ```
  - 正确格式：`{ "12,6": [[0, 6, "id", "right", "MT0", "MT0", 1, 6]] }`
  - 错误格式：`{ "12,6": [0, 6, "id", "right", "MT0", "MT0", 1, 6] }`

#### 问题3：移动方向错误
- **原因**：`rdir` 计算错误
- **排查**：检查 `core.turnDirection(":back", rd)` 的返回值

---

## 六、总结

### 6.1 修改要点

1. **`functions.js`**：
   - 移除错误的 `step.floorId == floorId` 条件
   - 使用 `.push([...])` 代替 `.concat([...])`，保持数组结构
   - 确保跨层阻击的 `repulse` 记录格式正确

2. **`control.js`**：
   - 删除跨层追猎代码（用户不需要）
   - 实现跨层阻击移动逻辑，支持同层推和跨面推
   - 添加错误处理，防止游戏卡死

### 6.2 技术亮点

1. **灵活的位置计算**：使用 `cubeStep()` 统一处理同层/跨层位置计算
2. **数据驱动**：通过 `repulse` 数组的长度判断是否为跨层阻击
3. **容错机制**：`try-catch` + 防御性检查，确保游戏稳定性

### 6.3 扩展建议

如果未来需要支持更多跨层功能（如跨层对话、跨层道具使用），可以参考本实现的思路：
1. 在 `functions.js` 中记录跨层信息
2. 在 `control.js` 中执行跨层逻辑
3. 使用 `cubeProject()` 和 `cubeStep()` 处理坐标转换

---

## 附录：相关函数索引

| 函数名 | 位置 | 功能 |
|--------|------|------|
| `cubeProject()` | `libs/controlData.js` | 计算坐标投影到哪个楼层 |
| `cubeStep()` | `libs/controlData.js` | 计算走一步后的实际位置 |
| `getCheckBlock()` | `project/functions.js` | 计算四周格子触发的事件 |
| `_checkBlock_repulse()` | `libs/control.js` | 执行阻击移动 |
| `core.turnDirection()` | `libs/core.js` | 计算方向 |
| `core.getBlock()` | `libs/core.js` | 获取地图格子数据 |

---

## 七、跨层追猎功能实现（新增）

### 7.1 功能概述
跨层追猎实现了怪物在相邻楼层向玩家移动的功能，遵循以下规则：
- 当怪物具有"追猎"特殊能力（special包含28）时
- 怪物和玩家处于**相邻楼层**的同一行或同一列
- 玩家在另一层同行或同列移动一格时，怪物也会往玩家方向移动一格
- 跨层追猎的检测范围跨越楼层，使用 `cubeStep` 逐步投影

### 7.2 典型场景示例
- **怪物位置**：MT0层 (12, 5)（右边缘）
- **玩家位置**：MT3层 y=5 行移动
- **触发条件**：玩家踩到MT3层y=5行的任意位置
- **预期效果**：
  - 怪物在MT0(12,5)向右移动一格
  - 由于已在边缘，跨面到MT3(0,5)

### 7.3 实现位置
- **检测跨层追猎触发**：`project/functions.js` 约1647-1676行（立方体跨面处理部分）
- **执行跨层追猎移动**：`libs/control.js` `_checkBlock_chase()` 约1356-1398行

### 7.4 核心代码

```javascript
// project/functions.js - 立方体跨面处理部分（约1647-1676行）
// 跨层追猎：遍历相邻楼层怪物，投影到当前层
if (core.hasSpecial(se.special, 28) && !core.hasFlag('no_chase')) {
    var _chaseDir = function(srcF, sx, sy, dir, floorId, chase) {
        var visited = {};
        var f = srcF, cx = sx, cy = sy;
        for (var step = 0; step < 200; step++) {
            var key = f + "," + cx + "," + cy;
            if (visited[key]) break;
            visited[key] = true;
            var s = core.control.controldata.cubeStep(f, cx, cy, dir);
            if (!s) break;
            f = s.floorId; cx = s.x; cy = s.y;
            if (f === floorId) {
                var ck = cx + "," + cy;
                if (!chase[ck]) chase[ck] = [];
                chase[ck].push({
                    x: sx, y: sy, dir: dir,
                    srcFloor: srcF
                });
            }
        }
    };
    _chaseDir(srcF, sb.x, sb.y, "left",  floorId, chase);
    _chaseDir(srcF, sb.x, sb.y, "right", floorId, chase);
    _chaseDir(srcF, sb.x, sb.y, "up",    floorId, chase);
    _chaseDir(srcF, sb.x, sb.y, "down",  floorId, chase);
}
```

```javascript
// libs/control.js - _checkBlock_chase() 跨层追猎执行（约1356-1398行）
if (srcFloor && srcFloor !== core.status.floorId) {
    var sm = core.status.maps[srcFloor];
    if (sm && sm.map && sm.map[y] && sm.map[y][x]) {
        var val = sm.map[y][x];
        // 怪物在源楼层向dir方向移动一格（可能跨面也可能不跨面）
        var stepTo = core.control.controldata.cubeStep(srcFloor, x, y, dir);
        
        if (stepTo && stepTo.floorId) {
            var dm = core.status.maps[stepTo.floorId];
            if (dm && dm.map && !core.getBlock(stepTo.x, stepTo.y, stepTo.floorId, false)) {
                dm.map[stepTo.y][stepTo.x] = val;
                sm.map[y][x] = 0;
                // 清除缓存
                if (sm.blocks) sm.blocks = null;
                if (dm.blocks) dm.blocks = null;
            }
        }
    }
    return;
}
```

### 7.5 立方体楼层关系（CUBE_EDGES）
```
MT0: 右边(x=12) → MT3左边
MT3: 左边(x=0)  → MT0右边

MT0(12,5)向右 → MT3(0,5)
MT3(0,5)向左  → MT0(12,5)
```

### 7.6 测试覆盖
- **单元测试**：`tests/control.test.js` - 验证跨层追猎移动逻辑
- **手动测试**：验证以下场景
  - 怪物在MT0(11,5)，玩家在MT3(0,5) → 怪物移动到MT0(12,5)
  - 怪物在MT0(12,5)，玩家在MT3(0,5) → 怪物跨面到MT3(0,5)

### 7.7 关键规则总结
| 条件 | 行为 |
|------|------|
| 玩家在另一层同行/同列移动一格 | 怪物向玩家方向移动一格 |
| 怪物未到边缘 | 在源楼层内移动 |
| 怪物已到边缘 | 跨面移动到相邻楼层 |

---

**文档版本**：v1.3  
**最后更新**：2026-06-27  
**作者**：AI Assistant
